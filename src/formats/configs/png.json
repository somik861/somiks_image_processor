{
  "loading_options": [],
  "saving_options": [
    {
      "type": "checkbox",
      "text": "Save faster with worse compression ratio",
      "default": false,
      "id": "fast_save"
    },
    {
      "type": "choice",
      "text": "Colorspace (number of channels of loaded image and selected colorspace must match)",
      "id": "colorspace",
      "values": [ "auto", "GRAY8", "GRAY8A", "RGB8", "RGBA8", "Linear GRAY8", "Linear GRAY8A", "Linear RGB8", "Linear RGBA8" ],
      "default": "auto"
    }
  ],
  "extensions": [
    {
      "suffix": "png"
    }
  ],
  "default_extension": "png"
}
